services:
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: pero_backend_app
  #   restart: unless-stopped
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   env_file:
  #     - .env
  #   environment:
  #     DATABASE_URL: file:./dev.db
  #     NODE_ENV: production
  #     WS_PORT: 8080
  #   ports:
  #     - "8080:8080"
